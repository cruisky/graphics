#include "UtilStdAfx.h"
#include "Math/MathUtil.h"

namespace TX{
	namespace Math{
		const float PI				= 3.14159265358979323846f;
		const float QTR_PI			= PI / 4.f;
		const float HALF_PI			= PI / 2.f;
		const float TWO_PI			= PI * 2.f;
		const float FOUR_PI			= PI * 4.f;
		const float PI_RCP			= 0.31830988618379067153f;
		const float HALF_PI_RCP		= PI_RCP * 2.f;
		const float TWO_PI_RCP		= PI_RCP / 2.f;
		const float FOUR_PI_RCP		= PI_RCP / 4.f;
		const float INF				= FLT_MAX;
		const float EPSILON			= FLT_EPSILON;
	}
}